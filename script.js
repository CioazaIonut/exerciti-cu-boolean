// Crearea variabilelor boolean și verificarea valorii:

let esteDisponibil = true;
let esteVindea = false;

console.log(esteDisponibil);
console.log(esteVindea);

// Verificare condiție simplă:

let arePermis = true;

console.log(arePermis === true);

// Comparare cu operatori:

let var1 = 10;
let var2 = 20;

console.log(var1 < var2);

// Combinație cu operatorul ! (negare):

let esteOnline = false;

console.log(!esteOnline);

// Verificare dacă două condiții sunt adevărate:

let varstaUnu = 25;
let detinePermis = true;

console.log(varstaUnu >= 18 && detinePermis);

// Exercițiu: Scrie o condiție care verifică dacă o persoană e adult și are permis:

let varsta = 30;
let permis = false;

console.log(varsta >= 18 && permis);

// Verificarea dacă un număr este par:

let numar = 8;
console.log(numar % 2 === 0);

// Crearea unei funcții care verifică dacă un copil poate merge la zoo:

let varstaCopil = 6;
let areBilet = true;
console.log(varstaCopil >= 5 && areBilet);

// Negarea unei condiții:

let utilizatorEsteAdmin = false;
console.log(!utilizatorEsteAdmin);

// Verificare dacă un obiect are anumite proprietăți true:

let esteStudent = true;
let areBursă = false;
console.log(esteStudent && areBursă);

// Test pentru condiție compusă:

let vremeFrumoasă = true;
let eWeekend = false;

// Verificare dacă două variabile de tip Boolean sunt diferite:

let a = true;
let b = false;
console.log(a !== b);

// Verifică dacă fiul are peste 18 ani:

let varstaFiu = 20;
console.log(varstaFiu >= 18);

// Fă o comparație pentru a afla dacă două variabile sunt egale:

let numar1 = 15;
let numar2 = 15;
console.log(numar1 === numar2);

// Verifică dacă un utilizator este major, dar nu are atestat:

let esteAdult = true;
let areAtestat = false;
console.log(esteAdult && arePermis);

// Testează dacă un număr este pozitiv sau negativ:

let primulNumar = -5;
console.log(primulNumar > 0);

// Negarea unei valori boolean:

let esteLaScoala = false;
console.log(!esteLaScoala);

// Verifică dacă o variabilă este null sau undefined:

let valoare = null;
console.log(valoare === null);
console.log(valoare === undefined);
